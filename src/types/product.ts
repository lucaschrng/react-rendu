export type Product = {
  id: string
  title: string,
  date: string,
  image: string,
  price: string,
  quantity: string,
  unit_of_measurement: string,
  measure: string,
  price_per_measure: string
}

export type Comment = {
  username: string,
  comment: string,
}